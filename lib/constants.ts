/**
 * Shared application-wide constants.
 *
 * Kept in `lib/` (rather than scattered across components) so later
 * phases — auth, claims, parsing, reporting — have one obvious place to
 * add their own constants alongside this one.
 */
export const APP_NAME = "Expense Claims";

export const APP_TAGLINE =
  "File a claim, get it signed off, get paid — without the paperwork.";
